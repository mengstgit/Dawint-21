<% layout ('layout') -%>
<div id="product-post">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="heading-section">
                    <h2>Feel free to send a message</h2>
                    <img src="images/under-heading.png" alt="">
                </div>
            </div>
        </div>
        <div id="contact-us">
            <div class="container">
                <div class="row">
                    <div class="product-item col-md-12">
                        <% if(messages.errors || messages.success_msg) { %>
                            <div class="row">
                                <div class="col-md-8">
                                    <% if(messages.errors) { %>
                                        <div class="alert alert-danger"> <%= messages.errors %> </div>
                                    <% } %>
                                    <% if(messages.success_msg) { %>
                                        <div class="alert alert-success"> <%= messages.success_msg %> </div>
                                    <% } %> 
                                </div>
                            </div>
                        <% } %> 
                        <div class="row">
                            <div class="col-md-8">
                                <div class="message-form">
                                    <form action="/send_message" method="post" class="send-message">
                                        <div class="row">
                                            <div class="name col-md-4">
                                                <input type="text" name="name" id="name" placeholder="Name" />
                                                    <% if(errors.length > 0 && errors.find(error => error.param === 'name')) { %>
                                                        <p class="text-danger"> <%= errors.find(error => error.param === 'name').msg %> </p>
                                                    <% } %>
                                            </div>
                                            <div class="email col-md-4">
                                                <input type="text" name="email" id="email" placeholder="Email" />
                                                    <% if(errors.length > 0 && errors.find(error => error.param === 'email')) { %>
                                                        <p class="text-danger"> <%= errors.find(error => error.param === 'email').msg %> </p>
                                                    <% } %>
                                            </div>
                                            <div class="subject col-md-4">
                                                <input type="text" name="subject" id="subject" placeholder="Subject" />
                                                    <% if(errors.length > 0 && errors.find(error => error.param === 'subject')) { %>
                                                        <p class="text-danger"> <%= errors.find(error => error.param === 'subject').msg %> </p>
                                                    <% } %>
                                            </div>
                                        </div>
                                        <div class="row">
                                            <div class="text col-md-12">
                                                <textarea name="message" placeholder="Message"></textarea>
                                                <% if(errors.length > 0 && errors.find(error => error.param === 'message')) { %>
                                                    <p class="text-danger"><%= errors.find(error => error.param === 'message').msg %> </p>
                                                <% } %>
                                            </div>
                                        </div>
                                        <div class="send">
                                            <button type="submit">Send</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="info">
                                    <p>Duis at pharetra neque, ut condimentum, purus nisl pretium quam, in pulvinar velit massa id elit. </p>
                                    <ul>
                                        <li><i class="fa fa-phone"></i>090-080-0760</li>
                                        <li><i class="fa fa-globe"></i>456 New Dagon City Studio, Yankinn, Digital Estate</li>
                                        <li><i class="fa fa-envelope"></i><a href="#">info@company.com</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="heading-section">
                    <h2>Find Us On Map</h2>
                    <img src="images/under-heading.png" alt="">
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div id="googleMap" style="height:340px;"></div>
            </div>
        </div>
    </div>
</div>

